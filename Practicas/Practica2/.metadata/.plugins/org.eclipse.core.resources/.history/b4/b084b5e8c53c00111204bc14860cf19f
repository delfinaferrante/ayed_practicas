package ej1;

public class ParcialArbolesEj7 {
	private BinaryTree<Integer> arbol;
	
	
	public boolean isLeftTree (int num) {
		if (this.arbol.isEmpty()) {
			return false;
		}
		
		// busco el nodo con la raiz 'num'
		if(this.arbol.getData() == num) {
			contarNodos(this.arbol);
		}
	}
}
