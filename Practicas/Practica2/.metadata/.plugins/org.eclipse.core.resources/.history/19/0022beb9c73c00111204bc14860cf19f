package ej1;

public class ParcialArbolesEj7 {
	private BinaryTree<Integer> arbol;
	
	
	public boolean isLeftTree (int num) {
		boolean esta = false;
		if (arbol == null) {
			return false;
		}
		
		// busco el nodo con la raiz 'num'
		if(arbol.getData() == num) {
			int cantIzq = contarNodos(arbol.getLeftChild());
			int cantDer = contarNodos(arbol.getRightChild());
		}
		
		if (arbol.hasLeftChild()) {
			esta = buscarValor(arbol.getLeftChild());
		}
		if(!esta) {
			
		}
	}
	
	private boolean buscarValor(BinaryTree<Integer> arbol) {
		
	}
	private int contarNodos(BinaryTree<Integer>arbol) {
		
	}
}
