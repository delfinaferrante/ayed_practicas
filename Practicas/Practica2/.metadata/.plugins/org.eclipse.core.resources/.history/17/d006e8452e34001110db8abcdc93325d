package ej1;

import java.util.ArrayList;
import java.util.List;

public class ContadorArbolEj3 {
	private BinaryTree<Integer> arbol;
	
	public ContadorArbolEj3(BinaryTree<Integer> arbol) {
		this.arbol = arbol;
	}
	
	public List<Integer> numerosPares() {
		List<Integer> listaPares = new ArrayList<>();
		this.InOrden(this.arbol, listaPares);
		//this.PostOrden(this.arbol, listaPares);
		return listaPares;
	}
	
	
	// hijo izquierdo, raiz, hijo derecho
	private void InOrden(BinaryTree<Integer> arbol, List<Integer> listaPares) {
		if (arbol.isEmpty() || arbol == null) {
			return;
		}
		
		if (arbol.hasLeftChild()) {
			this.InOrden(arbol.getLeftChild(), listaPares);
		}
		
		if (arbol.getData() % 2 == 0) {
			listaPares.add(arbol.getData());
		}
		
		if (arbol.hasRightChild()) {
			this.InOrden(arbol.getRightChild(), listaPares);
		}
	}
	
	// hijo izquierdo, hijo derecho, raiz
	private void PostOrden(BinaryTree<Integer> arbol, List<Integer> listaPares) {
		if (arbol.isEmpty() || arbol == null) {
			return;
		}
		
		if (arbol.hasLeftChild()) {
			this.PostOrden(arbol.getLeftChild(), listaPares);
		}
		
		if (arbol.hasRightChild()) {
			this.PostOrden(arbol.getRightChild(), listaPares);
		}
		
		if (arbol.getData() % 2 == 0) {
			listaPares.add(arbol.getData());
		}
	}
	
	
	public static void main(String[]args) {
		BinaryTree<Integer> ab = new BinaryTree<>(40);
		BinaryTree<Integer> hijoIzq = new BinaryTree<>(19);
		BinaryTree<Integer> hijoDer = new BinaryTree<>(30);
		
		ab.addLeftChild(hijoIzq);
		ab.addRightChild(hijoDer);
		
		
	}
}
